#  Template .NET Core build

#  This template allows you to validate your .NET Core package.
#  The workflow allows running tests and code linting on the default branch.

# To run your pipeline on a Windows machine, create a self-hosted Windows runner.
# For instructions on setting up a Windows runner, see https://support.atlassian.com/bitbucket-cloud/docs/set-up-runners-for-windows/

# image: mcr.microsoft.com/dotnet/sdk:8.0
image: docker:20.10.7

pipelines:
  default:
    - step:
        name: Build Docker Image
        services:
          - docker
        script:
          - docker build -t teste-automatizado/teste-automatizado .
    
    - step:
        name: Run Tests
        services:
          - docker
        script:
          - docker run --rm teste-automatizado/teste-automatizado dotnet test MeuClienteWebTestProject.csproj